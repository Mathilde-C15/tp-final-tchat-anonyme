<a href="#" class="create">+ Créer un salon</a>

<?php foreach ($rooms as $room): ?>
    <div class="room-item">
        <a href="index.php?page=room&id=<?= $room->getId() ?>">
            <?= htmlspecialchars($room->getName()) ?>
        </a>
    </div>
<?php endforeach ?>

<?php if (isset($error)): ?>
    <div class="error"><?= $error ?></div>
<?php endif ?>